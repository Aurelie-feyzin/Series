{% extends 'base.html.twig' %}

{% block title %}Edit Profil{% endblock %}

{% block main %}
    <div class="container">
        <h1 class="mb-3 font-weight-normal text-center">Modifier vos données personnelles</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
        <a class="btn btn-primary mt-3 text-center" href="{{ path('user_show', {id: user.id}) }}">Retour au Profil</a>
        <a href="{{ path('app_reset_password') }}" class="btn btn-primary mt-3 text-center">Modifier mot de passe</a>
        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
