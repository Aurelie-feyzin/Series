{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
<a class='btn btn-primary' href="{{ path('episode_show', {'id': comment.episode.id}) }}">Retourner à l'épisode</a>
{{ form_end(form) }}
